#include "main.h"

/**
 *
 * int_abs - Write a function that computes the absolute value of an integer.
 * @r: The character to check
 *
 * Return: -n or n
 */
int _abs(int n)
{
	if ( n < 0)
	{
		return (-n);
	}
	else
	{
		return (n);
	}
}	
